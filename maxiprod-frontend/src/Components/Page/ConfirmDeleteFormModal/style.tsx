import styled from "styled-components";
import { primary } from "../../../Commons/colors";

const ConfirmModalPromp = styled.p`
    margin: 20px 10px;
    font-size: 18px;
    color: ${primary};
`

export default ConfirmModalPromp