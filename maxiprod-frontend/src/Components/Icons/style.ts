import { secondary } from "../../Commons/colors";

const IconBaseStyle = {
    color: secondary,
    filter: `drop-shadow(3px 3px 2px rgba(0,0,0,0.6))`
}

const IconActionBaseStyle = {
    color: secondary,
    filter: `drop-shadow(3px 3px 2px rgba(0,0,0,0.6))`
}

export{ 
    IconBaseStyle,
    IconActionBaseStyle
}